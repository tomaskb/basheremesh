
ToDo List
-----------------------------------------------

* add Makefile tendist

* change config.mk for project.cfg and gendist it using patch?

* See the 'local' dir issue (do we need it?)

*.o : gendist? or clean ?

* Move gendist from main to .o rules 

Opportunelly:

* Check POSIX compliance

* Do we need four $ in recepit if enabled second expansion?

* Should we use $? in ar



Done
-------------------------------------------------

Most recent bellow.

* runcmd (const char* ...)

* Include COPYING etc. in runcmd and other subprojects (use gendist)

* Check if secondary expansion is posix (apparently not)

* Change install approach (no TEMPDIST)

* Fix TEMPDIST trailing blanks bug

* Use order-only prerrequisite in bin rule

* handle rpath (executable scritp)

* make dist : $(PROJECT).tgz ?







